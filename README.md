# graph-and-chain
Graph and chain generation for Quantifying Gerrymandering project.

Note: All Python files should be executed from the root directory, "graph-and-chain". So when executing, say, the "generate-dual-graph.py" file in the folder "graph", execute it from the project's root directory, not the "graph" directory.
